# Public identifiers, not secrets (WEBAPP_SPEC.md section 12.3). The
# publishable/anonymous key is safe to expose; RLS plus the database command
# boundary is the security boundary. Copy to .env.local for development.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
